language: java

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script: mvn clean install

after_success:
  - mvn clean test saga:coverage jacoco:report cobertura:cobertura coveralls:report
